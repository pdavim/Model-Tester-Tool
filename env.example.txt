# --- Server Configuration ---
# The port the application will listen on.
PORT=3767
# The port exposed on the host machine if running in Docker.
HOST_PORT=3767
# Node Environment (production, development, test)
NODE_ENV=production
# The public-facing URL of the application (required for CORS and Ingress).
APP_URL="http://localhost:3767"

# --- Provider API Keys ---
# OPENROUTER_API_KEY: Fallback key for OpenRouter API calls (if client doesn't provide one).
OPENROUTER_API_KEY="your_openrouter_api_key_here"
# HF_KEY: Fallback key for Hugging Face API calls (if client doesn't provide one).
HF_KEY="your_huggingface_api_key_here"

# --- Infrastructure ---
# REDIS_URL: Connection string for Redis. Required for rate-limiting and model info caching.
REDIS_URL="redis://localhost:6379"

# --- Logging & Monitoring ---
# (Optional) Log level (info, debug, warn, error)
LOG_LEVEL=info
# Prometheus metrics are enabled by default at /api/metrics
# Swagger documentation is enabled by default at /api/docs
